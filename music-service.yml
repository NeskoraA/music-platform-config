# music-service.yml в Git
server:
  port: 8081

spring:
  application:
    name: music-service

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.musicplatform.musicservice: DEBUG
    org.springframework.web.reactive: DEBUG

spotify:
  client:
    id: ca0b9e1b0a3645c9913c270259ec0aeb
    secret: 24df841663744a2a9bd2ee8dfa2b0118
  api:
    url: https://api.spotify.com/v1
  auth:
    url: https://accounts.spotify.com/api/token

eureka:
  instance:
    hostname: localhost
    prefer-ip-address: false
